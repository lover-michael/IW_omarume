import { Center, Box, Stack, Card } from "@chakra-ui/react";

export default function Loading() {
  return (
    <Center h="full" w="full" p={"7"}>
      <Box height={'xl'} bgColor={'gray.100'}>
      </Box>
    </Center>
  );
}
